/**
 * Clase de entidad que representa a Rol dentro del dominio de la aplicación.
 * Se utiliza para mapear la estructura de datos.
 */
export class Rol {
  /**
     * Propiedad de la entidad que representa id rol.
     * Contiene un valor de tipo: number.
     */
    id_rol: number;
  /**
     * Propiedad de la entidad que representa nombre rol.
     * Contiene un valor de tipo: string.
     */
    nombre_rol: string;
}
